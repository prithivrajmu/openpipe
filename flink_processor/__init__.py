"""Apache Flink (PyFlink) Stream Processor for Health Metrics.

Runs windowed aggregations inside a Flink cluster via the PyFlink DataStream API.
A standalone Python-threads fallback is available via the --standalone flag.
"""
